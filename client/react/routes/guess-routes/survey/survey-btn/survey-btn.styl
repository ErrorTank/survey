.survey-btn{
  border-radius 30px
  background-color spaColor
  color white
  font-size 1.4rem
  display inline-block
  padding 0.3rem 0.3rem
  text-align center
  font-weight bold
  min-width 18rem
  transition all 0.2s ease
  box-shadow:0px 1px 3px #696969
  &.disabled{
    opacity 0.8
    pointer-events none
  }
}